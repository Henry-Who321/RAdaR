# Special packages that require CUDA compilation or pre-installed PyTorch
# Install with: pip install -r requirements-special.txt --no-build-isolation
#
# IMPORTANT: Requires CUDA toolkit (nvcc) 12.x. Set CUDA_HOME if needed:
#   export CUDA_HOME=/usr/local/cuda-12.x
#
# If flash-attn fails, try:
#   pip install flash-attn==2.8.3 --no-build-isolation
#
# If xformers fails, try installing from PyTorch index:
#   pip install xformers==0.0.32.post1 --index-url https://download.pytorch.org/whl/cu128

cuda-bindings==13.0.3
cuda-pathfinder==1.3.2
cuda-python==13.0.3
cupy-cuda12x==13.6.0
flash_attn==2.8.3
flashinfer-cubin==0.5.0
flashinfer-python==0.5.0
sgl-kernel==0.3.17
sglang==0.5.5.post1
torchao==0.9.0
torchdata==0.11.0
vllm==0.11.0
xformers==0.0.32.post1
